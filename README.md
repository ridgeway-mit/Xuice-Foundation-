# Xuice-Foundation-

To set up your local project directory and push it to a new GitHub repository:

```bash
cd /Users/jalenridgeway/mediapipe_project
git remote add origin https://github.com/YOUR_USERNAME/mediapipe-pose-tracker.git
git branch -M main
git push -u origin main
```
